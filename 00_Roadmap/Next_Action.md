# Следующее действие

Не изменяя код, предложить структуру состояния для несортированного окна `[left..right]`, которая поддерживает добавление значения справа, удаление одного значения слева с учётом дубликатов и получение текущих min/max за `O(log n)` или быстрее.
