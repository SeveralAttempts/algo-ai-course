# Следующее действие

Не изменяя код, выполнить B2 для текущей реализации: описать модель и инвариант `SortedSet`/границ, почему удаляется именно `(values[left], left)`, доказать корректность результата, вывести время и память, назвать простую альтернативу и привести контрпример типичной неверной реализации. Отдельно объяснить, почему ветка `accept` вправе не сжимать недопустимое окно, длина которого уже не превосходит `bestValue.Len`.
